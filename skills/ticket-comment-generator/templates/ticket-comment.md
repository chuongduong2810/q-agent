# Ticket Comment / Bug Ticket

Choose ONE mode below and delete the other.

---

## Mode A — New Bug Ticket

**Title:** `<concise, specific: area + symptom, e.g. "Invoice — Pay Now stays disabled after all fields valid">`

**Summary**
`<1–2 sentences: what is broken and impact>`

**Environment**
- Application / URL: `<from knowledge.md>`
- Environment: `<Dev / QA / Staging / Prod>`
- Build / Branch / Commit: `<...>`
- Browser / OS: `<...>`

**Preconditions**
- `<state required before the steps, e.g. logged in as Agent, invoice exists>`

**Steps to Reproduce**
1. `<step>`
2. `<step>`
3. `<step>`

**Expected Result**
`<what should happen — tie to the Test Case's expected result>`

**Actual Result**
`<what actually happened>`

**Evidence / Attachments**
- Screenshot: `<link>`
- Trace: `<link>`
- Annotation notes: `<link>`

**Severity / Priority:** `<Severity 1–4 / Priority>`

**Linked Test Case / Acceptance Criteria:** `<TC-ID>` / `<AC-#>`

---

## Mode B — Progress / Update Comment

`<Short spoken-style paragraph: what was done, what was found, or what needs discussing.
Polite and direct, e.g. "I ran the invoice-payment suite against QA today. Most cases pass,
but TC-123 fails at the payment step — logging a defect now.">`

- `<key point / status bullet>`
- `<key point / open question>`
- `<next step or ask>`
