/**
 * PLACEHOLDER (#73 scaffold). The admin user-management screen (list users,
 * invite/create, change role, deactivate — via `api.auth.users` &c.) lands in
 * the admin slice, which replaces this file. Renders inside the app shell.
 */
export function UserManagement() {
  return (
    <div className="mx-auto max-w-[860px] py-10">
      <h1 className="m-0 mb-2 text-[26px] font-black tracking-[-0.03em]">User management</h1>
      <p className="m-0 text-[13.5px] text-muted">Admin user management coming soon.</p>
    </div>
  );
}
